[project]
name = "reserve-stream"
authors = ["ReserveStream Developers"]
description = "A comprehensive reserve tracking and management system"
telemetry = false
requirements = []
[contracts.reserve-stream]
path = "contracts/reserve-stream.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
